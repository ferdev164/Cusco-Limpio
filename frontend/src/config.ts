// Origen del backend: mismo host/protocolo que la pagina, puerto 3000.
// Asi funciona igual en localhost, en cusco.limpio y en la IP LAN (celular).
export const API_ORIGIN = `${window.location.protocol}//${window.location.hostname}:3000`;
